/** PongoLux's Google Merchant Center account ID. Used by both the
 * Customer Reviews survey opt-in module (checkout success page) and the
 * Customer Reviews badge (site-wide, root layout) — one place to update
 * if this ever changes. Find/confirm it in Merchant Center's top-right
 * account switcher, or in the URL of any Merchant Center page
 * (merchants.google.com/mc/...?a=<merchant_id>). */
export const GOOGLE_MERCHANT_ID = 5397913684;
